#
#---- [ OPEN ] ----
#
root/includes/abbcode.php
#
#---- [ FIND ] ----
#
					$linktest_return	= $linktest->test($linktest_value);
#
#---- [ REPLACE WITH ] ----
#
					// if there is no scheme, then add http schema
					if (!preg_match('#^[a-z][a-z\d+\-.]*:/{2}#i', $linktest_value))
					{
						$linktest_value = 'http://' . $linktest_value;
					}

					$linktest_return	= $linktest->test($linktest_value);
